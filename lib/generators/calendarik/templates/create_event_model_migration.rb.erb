class Create<%= migration_class_name %> < ActiveRecord::Migration
  def change
    create_table :<%= table_name %> do |t|
<% additional_attributes.each do |attribute| -%>
      t.<%= attribute.split(":")[1] %> :<%= attribute.split(":")[0] %>
<% end -%>
      t.datetime :start_time
      t.datetime :end_time
      t.boolean  :all_day,  :default => false
      t.boolean  :free_day,  :default => false
      t.integer  :event_series_label_timestamp
      t.string   :title
      t.text     :description
      t.timestamps null: false
    end
  end
end
